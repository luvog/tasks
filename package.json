{
  "name": "@luvog/tasks",
  "version": "0.0.0",
  "description": "Wrapper class for scheduling timers in the node's event loop capable of being paused, resumed, terminated before time or rescheduled before or after end",
  "main": "./lib/cjs/index.js",
  "module": "./lib/esm/index.js",
  "files": [
    "lib/"
  ],
  "scripts": {
    "tsc": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "prepublishOnly": "npm run tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lvolivaresgomez@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.9",
    "typescript": "^3.9.3"
  }
}
